# This is used to run the configuration changes

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.9.2

inputs:
  - name: git-stegeler

outputs:
  - name: run-errors.log

run:
  path: git-stegeler/ci/scripts/run-config.sh
